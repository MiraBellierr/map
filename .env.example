# Discord Bot Configuration
DISCORD_TOKEN=YOUR_DISCORD_TOKEN_HERE

# Main Guild IDs
DCOLON=873441703330185247
DCOLON_CHANNEL=873441703330185250

# Ollama Configuration
OLLAMA_API_URL=http://localhost:11434/api
OLLAMA_CHAT_MODEL=llama2
OLLAMA_IMAGE_MODEL=llava

# GPU Configuration (Ollama)
# Set to "false" to disable GPU and use CPU only
OLLAMA_GPU_ENABLED=true

# Number of layers to offload to GPU (-1 = all layers)
# Set between 0-99 to control GPU usage, -1 for maximum GPU acceleration
OLLAMA_GPU_LAYERS=-1

# Number of threads for CPU operations (0 = auto-detect)
OLLAMA_NUM_THREADS=0

# Number of GPUs to use (1 = single GPU, multiple = multi-GPU setup)
OLLAMA_NUM_GPU=1

